# Copy to .env and fill in. Load with: set -a; source .env; set +a
OPENAI_API_KEY=sk-...
# Optional: any OpenAI-compatible endpoint (vLLM, a relay service, etc.)
# OPENAI_BASE_URL=https://api.openai.com/v1
